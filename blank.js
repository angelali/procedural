function setup() {
  makeCanvas();

  textSize(32);
  textFont('monospace');
  text('whew!', width * 0.7, height * 0.1);
  text('what', width * 0.1, height * 0.3);
  text('a', width * 0.5, height * 0.6);
  text('workout', width * 0.6, height * 0.7);

  textSize(12);
  text("let's all cool down for a bit", width * 0.2, height * 0.8);
}

function draw() {

}
