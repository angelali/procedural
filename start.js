function setup() {
  makeCanvas();

  textSize(32);
  textFont('monospace');
  text('hi', width * 0.7, height * 0.1);

  textSize(12);
  text(":)", width * 0.2, height * 0.8);
}

function draw() {

}
